spring:
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: config
      fail-fast: true

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://admin:123456@localhost:8761/eureka/